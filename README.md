# property24-project
This projects depicts how l created a database in SQL for the Property24 dataset,then created a table with columns that match the dataset and assigned appropriate data types to each column.
